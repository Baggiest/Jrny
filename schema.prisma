generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url      = "file:./entries.db"
}

model Entry {
    id   Int     @id @default(autoincrement())
    Hash  String?
    text String?
}

// UID: number,
// index?: number,
// text?: string,
// isEncrypted?: boolean,
// moodLevel?: number
